#!/bin/bash
[ -f .env ] && source .env

# export S3_INPUT_PATH="s3://nesta-ds-projects/<MISSION OR PRACTICE>/<PROJECT NAME>"

# Activate the conda environment
eval "$(conda shell.bash activate "asf_next_three_million_heat_pump_owners")"
[ "$CONDA_DEFAULT_ENV" = "asf_next_three_million_heat_pump_owners" ] || echo "Have you run \`make install\`?"
